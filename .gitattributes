# Standardize all text files to use LF line endings in the repository.
# On checkout, Git will convert to native line endings (CRLF on Windows).
* text=auto eol=lf

# Force specific file types to be treated as binary to prevent any conversion.
# This prevents corruption of non-text assets like images and fonts.
*.jpg binary
*.jpeg binary
*.png binary
*.gif binary
*.ico binary
*.svg binary
*.pdf binary
*.woff binary
*.woff2 binary
*.eot binary
*.ttf binary
*.otf binary